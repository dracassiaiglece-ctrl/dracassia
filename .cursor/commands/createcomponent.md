# createcomponent

Create a new React component following this project's standards.

Rules:
- TypeScript only
- Tailwind CSS for styling
- Clean structure
- Mobile-first
- No unnecessary abstractions

The component must be ready for production use.


This command will be available in chat with /createcomponent
